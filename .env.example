# AgentCtl & Agency-QuickDeploy Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# REQUIRED: GCP Configuration
# ============================================================================
# Your Google Cloud Project ID
QUICKDEPLOY_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# ============================================================================
# AUTHENTICATION: Choose ONE method
# ============================================================================

# Method 1: Anthropic API Key (pay-per-token)
# Get your key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-...

# Method 2: Claude Code OAuth Token (subscription-based)
# Generate with: claude setup-token
# CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-...

# ============================================================================
# OPTIONAL: GCP Settings
# ============================================================================
# GCP zone (default: us-central1-a)
# QUICKDEPLOY_ZONE=us-central1-a

# GCS bucket for agent data (auto-generated if not set)
# QUICKDEPLOY_BUCKET=my-bucket-name

# ============================================================================
# OPTIONAL: Agency-QuickDeploy Settings
# ============================================================================
# Authentication type: 'api_key' or 'oauth' (default: api_key)
# QUICKDEPLOY_AUTH_TYPE=api_key

# Deployment provider: 'gcp' or 'railway' (default: gcp)
# QUICKDEPLOY_PROVIDER=gcp

# ============================================================================
# RAILWAY PROVIDER CONFIGURATION
# ============================================================================
# Railway API token (get from https://railway.com/account/tokens)
# Required if using --provider railway
# RAILWAY_TOKEN=your-railway-api-token

# Railway project ID (optional - creates new project if not set)
# RAILWAY_PROJECT_ID=your-project-id

# Custom Docker image for agent (optional)
# RAILWAY_AGENT_IMAGE=ghcr.io/anthropics/agency-quickdeploy-agent:latest

# ============================================================================
# OPTIONAL: AgentCtl Settings
# ============================================================================
# Master server URL (for agentctl CLI)
# AGENTCTL_MASTER_URL=http://localhost:8000

# GCP region for agentctl (default: us-central1)
# AGENTCTL_GCP_REGION=us-central1

# GCP zone for agentctl (default: us-central1-a)
# AGENTCTL_GCP_ZONE=us-central1-a
